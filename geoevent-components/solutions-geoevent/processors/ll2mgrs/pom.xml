<?xml version="1.0" encoding="UTF-8"?>
<project
        xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
>
  <!-- Licensed to the Apache Software Foundation (ASF) under one or more contributor
          license agreements. See the NOTICE file distributed with this work for additional
          information regarding copyright ownership. The ASF licenses this file to You under
          the Apache License, Version 2.0 (the "License"); you may not use this file except
          in compliance with the License. You may obtain a copy of the License at http://www.apache.org/licenses/LICENSE-2.0
          Unless required by applicable law or agreed to in writing, software distributed under
          the License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS
          OF ANY KIND, either express or implied. See the License for the specific language
          governing permissions and limitations under the License. -->
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.esri.geoevent.processor</groupId>
  <artifactId>latlong-mgrs-converter-processors</artifactId>
  <version>${revision}</version>
  <packaging>bundle</packaging>
  <name>LatLong - MGRS Converter Processors Blueprint Bundle</name>
  <description>LatLong - MGRS Converter Processors OSGi blueprint bundle project.</description>
  <url>https://www.arcgis.com/home/item.html?id=${gallery.itemId}</url>
  <organization>
    <name>Esri</name>
    <url>https://www.esri.com</url>
  </organization>
  <properties>
    <project.release>3</project.release>
    <compatibility.min>${project.version}</compatibility.min>
    <compatibility.max></compatibility.max>
    <build.time>${maven.build.timestamp}</build.time>
    <gallery.itemId>f34314728ae04664925dfb86ebc0bc3f</gallery.itemId>
    <release.itemId>951159ded3794040b932dbeff3860db5</release.itemId>
    <release.name>latlong-mgrs-converter</release.name>
    <revision>10.8.1</revision>
    <contact.address>geoevent@esri.com</contact.address>
    <project.build.properties.encoding>ISO-8859-1</project.build.properties.encoding>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Override with -Dgeoevent.home=... when GeoEvent is installed elsewhere. -->
    <geoevent.home>C:/Program Files/ArcGIS/Server/GeoEvent</geoevent.home>
    <!--
      GeoEvent's geometry-api bundle exports com.esri.* at the product version
      (12.1.0, 12.2.0, ...), so any upper bound in Import-Package leaves this
      bundle unresolvable after a product upgrade. Release 2 shipped
      version="[10.6,12)" and stopped resolving at 12.1. Keep this open ended.
    -->
    <esri.package.version.range>0.0.0</esri.package.version.range>
    <!--
      JDK 8 cannot read the GeoEvent 12.x SDK jar. Raise java.build.version to
      match the JDK that built the targeted SDK (25 for 12.2). release 8 keeps
      the emitted bundle portable across GeoEvent runtimes.
    -->
    <java.build.version>11</java.build.version>
    <java.release>8</java.release>
    <!-- maven-bundle-plugin 6.x requires Maven 3.8.1 or newer. -->
    <maven.minimum.version>3.8.1</maven.minimum.version>
    <maven.bundle.plugin.version>6.1.2</maven.bundle.plugin.version>
    <maven.compiler.plugin.version>3.16.0</maven.compiler.plugin.version>
    <maven.enforcer.plugin.version>3.6.3</maven.enforcer.plugin.version>
    <maven.resources.plugin.version>3.5.0</maven.resources.plugin.version>
    <maven.surefire.plugin.version>3.5.6</maven.surefire.plugin.version>
    <junit.version>4.13.2</junit.version>
    <easymock.version>5.7.0</easymock.version>
  </properties>
  <repositories>
    <repository>
      <id>ages-sdk-repo</id>
      <name>GeoEvent Processor SDK repository</name>
      <url>file:///${geoevent.home}/sdk/repository</url>
    </repository>
    <repository>
      <id>GEP</id>
      <name>GeoEvent Processor embedded repository</name>
      <layout>default</layout>
      <url>file:///${geoevent.home}/System</url>
    </repository>
  </repositories>
  <dependencies>
    <dependency>
      <groupId>com.esri.geoevent.sdk</groupId>
      <artifactId>geoevent-sdk</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- GeoEvent's geometry-api bundle supplies the projection engine (com.esri.sde.sdk.pe.*). -->
    <dependency>
      <groupId>com.esri.geometry</groupId>
      <artifactId>geometry-api</artifactId>
      <version>${project.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.easymock</groupId>
      <artifactId>easymock</artifactId>
      <version>${easymock.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.plugin.version}</version>
          <configuration>
            <release>${java.release}</release>
            <encoding>${project.build.sourceEncoding}</encoding>
            <compilerArgs>
              <arg>-Xlint:-options</arg>
            </compilerArgs>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven.enforcer.plugin.version}</version>
        <executions>
          <execution>
            <id>enforce-build-toolchain</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[${maven.minimum.version},)</version>
                </requireMavenVersion>
                <requireJavaVersion>
                  <version>[${java.build.version},)</version>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>${maven.resources.plugin.version}</version>
        <configuration>
          <propertiesEncoding>${project.build.properties.encoding}</propertiesEncoding>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <version>${maven.bundle.plugin.version}</version>
        <extensions>true</extensions>
        <configuration>
          <finalName>${project.artifactId}-${project.version}</finalName>
          <instructions>
            <AGES-Gallery>TRUE</AGES-Gallery>
            <AGES-Release>${project.release}</AGES-Release>
            <AGES-Domain>com.esri.geoevent.solutions.processor</AGES-Domain>
            <Bundle-Version>${project.version}.${project.release}</Bundle-Version>
            <Bundle-SymbolicName>${project.groupId}.${project.artifactId}</Bundle-SymbolicName>
            <Bundle-ContactAddress>${contact.address}</Bundle-ContactAddress>
            <Export-Package />
            <Private-Package>com.esri.geoevent.solutions.processor.ll2mgrs</Private-Package>
            <Import-Package>com.esri.sde.sdk.pe.engine;version="${esri.package.version.range}",
              com.esri.sde.sdk.pe.factory;version="${esri.package.version.range}",
              com.esri.core.geometry;version="${esri.package.version.range}",*</Import-Package>
          </instructions>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
